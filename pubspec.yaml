name: sangak
description: "Fresh Persian bread delivered to your door."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.2.0+5

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # Navigation
  go_router: ^14.2.1

  # State Management
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  # Backend & Auth
  supabase_flutter: ^2.5.10
  google_sign_in: ^6.2.1

  # UI & Design
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  cupertino_icons: ^1.0.8
  cached_network_image: ^3.3.1

  # Localization & Storage
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  shared_preferences: ^2.2.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.3

  # Utilities
  http: ^1.2.1
  package_info_plus: ^8.0.0
  url_launcher: ^6.3.0
  pub_semver: ^2.1.4
  image_picker: ^1.1.2
  flutter_dotenv: ^5.1.0
  geolocator: ^13.0.1
  geocoding: ^3.0.0

  # SVG support
  flutter_svg: ^2.0.10+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  riverpod_generator: ^2.4.0
  build_runner: ^2.4.9
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1
  hive_generator: ^2.0.1

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "lib/assets/icon/icon.png"
  min_sdk_android: 21
  remove_alpha_ios: true

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true
  generate: true

  assets:
    - lib/assets/branding/
    - lib/assets/icon/
    - .env

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  fonts:
    - family: IRANYekanX
      fonts:
        - asset: lib/assets/fonts/IranYekan/fonnts.com-Qs_Iranyekan-thin.ttf
          weight: 100
        - asset: lib/assets/fonts/IranYekan/fonnts.com-Qs_Iranyekan-light.ttf
          weight: 300
        - asset: lib/assets/fonts/IranYekan/fonnts.com-Qs_Iranyekan.ttf
          weight: 400
        - asset: lib/assets/fonts/IranYekan/fonnts.com-Qs_Iranyekan-medium.ttf
          weight: 500
        - asset: lib/assets/fonts/IranYekan/fonnts.com-Qs_Iranyekan-bold.ttf
          weight: 700
        - asset: lib/assets/fonts/IranYekan/fonnts.com-Qs_Iranyekan-extrabold.ttf
          weight: 800
        - asset: lib/assets/fonts/IranYekan/fonnts.com-Qs_Iranyekan-black.ttf
          weight: 900
